var price = 55;
// price = price + 20;

// price += 20; // price = price + 20;
price -= 20; // price = price - 20;

// console.log(price);

// Multiply
var mangoPrice = 34;
// mangoPrice = mangoPrice * 3;

mangoPrice *= 3; // mangoPrice = mangoPrice * 3;

// console.log(mangoPrice);

// Division

var book = 64;
// book = book / 3;

book /= 3;

console.log(book);